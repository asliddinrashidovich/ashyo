export interface sliderDataType {
    id: number
    image: string
    title: string
    paragraph: string
    link: string
}

export interface heroCategoriesData {
    createdAt: string
    icon: string
    _id: number
    image: string
    name: string
    updatedAt: string
}